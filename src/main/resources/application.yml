server:
  port: 7001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clientes?createDatabaseIfNotExist=true
    username: root
    password: admin
    
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true    